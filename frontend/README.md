# Frontend

React + TypeScript + Vite UI for the Local AI Document Workspace.

```
npm install
npm run dev      # http://127.0.0.1:5173 (proxies /api to the backend on :8000)
npm run build    # type-check + production build
npm run lint
```

See the top-level README for the full setup.
